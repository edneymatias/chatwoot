# Phase 1 Data Model: Kanban Card Action Footer

**Status**: N/A — no data entities are introduced or modified by this feature.

This is a presentational/layout-only change to `KanbanCard.vue`. It consumes the existing
`opportunity` prop (already defined and unchanged) and the existing `currentStage` Vuex getter
result; no new fields, attributes, relationships, or state transitions are introduced.
